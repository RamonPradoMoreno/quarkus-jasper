package org.acme.events;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends EmployeesResourceTest {

    // Execute the same tests but in native mode.
}